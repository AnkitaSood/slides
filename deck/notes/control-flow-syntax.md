---
slide: control-flow-syntax
---
Frame this as foundational, not new — control flow syntax has been stable since v17, so most of the room already knows it.

The pitch: no more `CommonModule` imports, and the block-based syntax reads closer to plain JavaScript than the structural directives it replaces.